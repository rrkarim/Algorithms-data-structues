#ifndef __DIJKSTRA__
#define __DIJKSTRA__

template <typename T>
using vii = std::vector< vector<T> >;

template <typename T>
using vi = std::vector<T>;

namespace alg {
	template <typename T>
	class DJpath {
		public:
			static void run(const vii<T> & g, const T init) {

			}
	};


}

#endif